Name:			simple-obfs
Version:        0.0.3
Release:        1.17.git0feb364%{?dist}
Summary:		A simple obfuscating tool

Group:			Applications/Internet
License:		GPLv3+
URL:			https://github.com/shadowsocks/%{name}
Source0:        simple-obfs-0.0.3.tar.gz
AutoReq:		no

BuildRequires:	gcc make automake autoconf libtool zlib-devel openssl-devel libsodium-devel >= 1.0.4 c-ares-devel asciidoc xmlto
Requires:		zlib openssl libsodium >= 1.0.4 c-ares

%description
Simple-obfs is a simple obfusacting tool, designed as plugin server of shadowsocks.

%prep
%setup -q

%build
./autogen.sh

%configure --enable-shared

make %{?_smp_mflags}

%install
make install DESTDIR=%{buildroot}


%files
%defattr(-,root,root)
%{_bindir}/*


%changelog
